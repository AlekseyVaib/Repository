# 🚀 Развертывание Валидатора V3 на Deploy-F

## 📋 Что такое Deploy-F?

Deploy-F — российский хостинг для Python приложений, позволяющий развернуть Flask-приложение без сложной настройки сервера.

> **Перед деплоем:** откройте **ПОДГОТОВКА_К_ДЕПЛОЮ_DEPLOY-F.md** — там актуальный чеклист и список файлов с учётом всех изменений в коде.

## ✅ Подготовка файлов

### Шаг 1: Проверьте структуру проекта

Убедитесь, что у вас есть все необходимые файлы (см. `СПИСОК_ФАЙЛОВ.txt`):

```
Валидатор/
├── app.py                    # Flask приложение
├── email_validator.py        # Ваш валидатор (не изменяем)
├── requirements_web.txt     # Зависимости
├── Procfile                  # Команда запуска
├── runtime.txt              # Версия Python
├── templates/
│   └── index.html           # Веб-интерфейс
└── static/
    ├── css/
    │   └── style.css       # Стили
    └── js/
        └── main.js         # JavaScript
```

### Шаг 2: Подготовьте Procfile

Убедитесь, что файл `Procfile` содержит:

```
web: gunicorn -w 4 -b 0.0.0.0:$PORT --timeout 300 app:app
```

### Шаг 3: Проверьте runtime.txt

Убедитесь, что файл `runtime.txt` содержит версию Python:

```
python-3.9.18
```

Или другую поддерживаемую версию (3.9, 3.10, 3.11).

## 🌐 Развертывание на Deploy-F

### Вариант A: Через веб-интерфейс Deploy-F

#### Шаг 1: Регистрация и вход

1. Перейдите на сайт Deploy-F: **https://deploy-f.ru** (или другой адрес, если изменился)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в панель управления

#### Шаг 2: Создание нового приложения

1. Нажмите кнопку **"Создать приложение"** или **"New App"**
2. Выберите тип: **Python/Flask**
3. Введите название приложения (например: `validator-v3`)

#### Шаг 3: Загрузка файлов

**Способ 1: Через Git (рекомендуется)**

1. Создайте Git репозиторий (GitHub, GitLab, Bitbucket)
2. Загрузите все файлы проекта в репозиторий
3. В Deploy-F выберите **"Подключить Git репозиторий"**
4. Введите URL вашего репозитория
5. Выберите ветку (обычно `main` или `master`)

**Способ 2: Прямая загрузка**

1. В Deploy-F выберите **"Загрузить файлы"**
2. Создайте ZIP архив со всеми файлами проекта (кроме `venv/`, `__pycache__/`, `*.log`)
3. Загрузите архив через веб-интерфейс
4. Deploy-F автоматически распакует файлы

#### Шаг 4: Настройка приложения

1. **Укажите точку входа:**
   - Файл запуска: `app.py`
   - Или используйте `Procfile` (если поддерживается)

2. **Установите зависимости:**
   - Deploy-F автоматически найдет `requirements_web.txt`
   - Или укажите вручную: `pip install -r requirements_web.txt`

3. **Настройте переменные окружения:**
   - Перейдите в раздел **"Переменные окружения"** или **"Environment Variables"**
   - Добавьте:
     ```
     SECRET_KEY=ваш-секретный-ключ-измените-это
     FLASK_ENV=production
     PORT=5000
     ```

#### Шаг 5: Настройка папок

Убедитесь, что папки `uploads/` и `results/` будут созданы автоматически при первом запуске.

Если Deploy-F требует создания папок вручную:
1. Создайте папку `uploads/`
2. Создайте папку `results/`

#### Шаг 6: Запуск приложения

1. Нажмите кнопку **"Развернуть"** или **"Deploy"**
2. Дождитесь завершения процесса (обычно 2-5 минут)
3. Проверьте логи развертывания на наличие ошибок

#### Шаг 7: Получение URL

После успешного развертывания вы получите URL вида:
- `https://your-app-name.deploy-f.ru`
- Или `https://your-app-name.deploy-f.com`

### Вариант B: Через SSH (если доступен)

Если Deploy-F предоставляет SSH доступ:

1. **Подключитесь к серверу:**
   ```bash
   ssh username@your-server.deploy-f.ru
   ```

2. **Перейдите в папку приложения:**
   ```bash
   cd /path/to/your/app
   ```

3. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

4. **Установите зависимости:**
   ```bash
   pip install -r requirements_web.txt
   pip install gunicorn
   ```

5. **Запустите приложение:**
   ```bash
   gunicorn -w 4 -b 0.0.0.0:5000 --timeout 300 app:app
   ```

## 🔧 Настройка для Deploy-F

### Обновление app.py для Deploy-F

Убедитесь, что в `app.py` используется правильный порт:

```python
if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5000))
    app.run(host='0.0.0.0', port=port, debug=False)
```

Или используйте Gunicorn через Procfile (рекомендуется).

### Настройка путей к файлам

Если Deploy-F использует специфичные пути, обновите в `app.py`:

```python
# Для Deploy-F может потребоваться абсолютные пути
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
app.config['UPLOAD_FOLDER'] = os.path.join(BASE_DIR, 'uploads')
app.config['RESULTS_FOLDER'] = os.path.join(BASE_DIR, 'results')
```

## ✅ Проверка работы

### Шаг 1: Откройте приложение

1. Перейдите по URL, который предоставил Deploy-F
2. Вы должны увидеть интерфейс валидатора

### Шаг 2: Протестируйте функционал

1. **Загрузка файла:**
   - Нажмите "Выбрать файл" или перетащите файл
   - Выберите тестовый файл с email адресами (.xlsx, .xls, .csv)

2. **Настройки:**
   - Выберите режим валидации (Строгий/Лояльный)
   - Настройте параметры (SMTP, таймаут, и т.д.)

3. **Запуск валидации:**
   - Нажмите "Начать проверку"
   - Дождитесь завершения (отслеживайте прогресс)

4. **Скачивание результата:**
   - После завершения нажмите "Скачать результат"
   - Файл должен скачаться

## 🔍 Решение проблем

### Проблема 1: Приложение не запускается

**Решение:**
1. Проверьте логи в панели Deploy-F
2. Убедитесь, что все зависимости установлены
3. Проверьте, что `app.py` существует и корректен
4. Убедитесь, что указан правильный файл запуска

### Проблема 2: Ошибка импорта модулей

**Решение:**
1. Проверьте `requirements_web.txt` - все зависимости должны быть указаны
2. Убедитесь, что `email_validator.py` находится в той же папке, что и `app.py`
3. Проверьте логи установки зависимостей

### Проблема 3: Файлы не загружаются

**Решение:**
1. Проверьте права на папки `uploads/` и `results/`
2. Убедитесь, что папки созданы
3. Проверьте максимальный размер файла (50 MB в настройках)

### Проблема 4: Валидация не работает

**Решение:**
1. Проверьте логи: `email_validator.log`
2. Убедитесь, что сервер имеет доступ в интернет (для DNS/SMTP проверок)
3. Проверьте таймауты в настройках

### Проблема 5: Ошибка 500 или Internal Server Error

**Решение:**
1. Проверьте логи в панели Deploy-F
2. Убедитесь, что все переменные окружения установлены
3. Проверьте синтаксис Python кода
4. Убедитесь, что все файлы загружены корректно

## 📊 Мониторинг и логи

### Просмотр логов в Deploy-F

1. Перейдите в панель управления
2. Выберите ваше приложение
3. Откройте раздел **"Логи"** или **"Logs"**
4. Просматривайте логи в реальном времени

### Локальные логи

Приложение также создает файл `email_validator.log` в корне проекта.

## 🔄 Обновление приложения

### Способ 1: Через Git

1. Внесите изменения в код локально
2. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```
3. Deploy-F автоматически обнаружит изменения и переразвернет приложение

### Способ 2: Через веб-интерфейс

1. Загрузите обновленные файлы через веб-интерфейс
2. Нажмите "Переразвернуть" или "Redeploy"

## 🔒 Безопасность

### Обязательные настройки:

1. **Измените SECRET_KEY:**
   ```python
   SECRET_KEY=сгенерируйте-случайную-длинную-строку
   ```

2. **Используйте HTTPS:**
   - Deploy-F обычно предоставляет SSL сертификат автоматически
   - Убедитесь, что используется HTTPS

3. **Ограничьте размер файлов:**
   - Уже настроено: 50 MB максимум
   - Можно изменить в `app.py`:
     ```python
     app.config['MAX_CONTENT_LENGTH'] = 50 * 1024 * 1024
     ```

4. **Регулярно очищайте файлы:**
   - Настройте автоматическую очистку старых файлов
   - Или делайте это вручную через панель управления

## 📝 Чеклист развертывания

- [ ] Все файлы проекта загружены
- [ ] `Procfile` создан и корректен
- [ ] `runtime.txt` указан
- [ ] `requirements_web.txt` содержит все зависимости
- [ ] Переменные окружения настроены
- [ ] Папки `uploads/` и `results/` созданы (или будут созданы автоматически)
- [ ] Приложение успешно развернуто
- [ ] URL получен и работает
- [ ] Протестирована загрузка файлов
- [ ] Протестирована валидация
- [ ] Протестировано скачивание результатов
- [ ] Логи проверены на ошибки

## 🆘 Поддержка Deploy-F

Если возникли проблемы:

1. **Проверьте документацию Deploy-F:**
   - Обычно доступна в разделе "Документация" или "Help"

2. **Обратитесь в поддержку:**
   - Через тикет-систему в панели управления
   - Или по email поддержки

3. **Проверьте статус сервиса:**
   - Убедитесь, что Deploy-F работает без сбоев

## 📞 Дополнительная информация

- **Файл со списком всех файлов:** `СПИСОК_ФАЙЛОВ.txt`
- **Техническая документация:** `DEPLOYMENT.md`
- **Быстрый старт:** `QUICK_START.md`

---

**Успешного развертывания на Deploy-F! 🚀**
