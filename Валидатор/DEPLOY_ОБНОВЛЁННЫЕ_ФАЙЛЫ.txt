================================================================================
ОБНОВЛЁННЫЕ ФАЙЛЫ ВЕБ-ВЕРСИИ (приведение к GUI Валидатор V3)
================================================================================
Используйте этот список для частичного обновления на сервисе — замените только
указанные файлы, без полной перезаливки архива.

ОБНОВЛЁННЫЕ ФАЙЛЫ:
------------------
1. templates/index.html
   - Заголовок: «Валидатор V3» (title и h1)
   - Убрана опция «Выполнять SMTP проверку» (SMTP всегда включён, как в GUI)

2. static/css/style.css
   - Цвета и стиль как в GUI: фон #F8FAFC, панели #F1F5F9, акцент #2563EB
   - Заголовок: компактный, синий (#2563EB)
   - Карточки: панельный стиль, граница #E2E8F0
   - Кнопки, прогресс-бар, статусы и инфо-блок в единой палитре
   - Шрифт: Segoe UI, Arial

3. static/js/main.js
   - Отправка check_smtp всегда true (опция SMTP убрана из формы)

НЕ МЕНЯЛИСЬ (уже соответствуют GUI):
------------------------------------
- app.py          — прогресс (current_file, processed, total, eta_seconds), 
                    accept_catch_all=False, progress_callback уже были
- email_validator.py — ETA с 5-й почты, catch-all → X, в коде уже есть

КАК ОБНОВИТЬ НА СЕРВИСЕ:
------------------------
Вариант A (частично):
  - Загрузить только: templates/index.html, static/css/style.css, static/js/main.js
  - Остальные файлы (app.py, email_validator.py и т.д.) не трогать

Вариант B (полный деплой):
  - Заменить весь архив по списку из ПОДГОТОВКА_К_ДЕПЛОЮ_DEPLOY-F.md

================================================================================
Дата: актуально для Валидатор V3 (все изменения из GUI перенесены в веб).
================================================================================
