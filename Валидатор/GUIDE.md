# Полное руководство по использованию Email Валидатора

## Содержание
1. [Установка Python](#установка-python)
2. [Установка PyCharm (рекомендуется)](#установка-pycharm-рекомендуется)
3. [Установка необходимых модулей](#установка-необходимых-модулей)
4. [Подготовка файла с email адресами](#подготовка-файла-с-email-адресами)
5. [Запуск программы](#запуск-программы)
6. [Понимание результатов](#понимание-результатов)
7. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Установка Python

### Шаг 1: Скачивание Python

1. Откройте браузер и перейдите на официальный сайт Python: **https://www.python.org/downloads/**
2. Нажмите на большую желтую кнопку **"Download Python"** (скачать последнюю версию)
3. Или выберите конкретную версию (рекомендуется Python 3.8 или новее)

### Шаг 2: Установка Python

1. Запустите скачанный установщик (файл `.exe`)
2. **ВАЖНО**: В самом начале установки установите галочку **"Add Python to PATH"** (Добавить Python в PATH)
   - Это позволит запускать Python из командной строки
3. Нажмите **"Install Now"** (Установить сейчас)
4. Дождитесь завершения установки
5. Нажмите **"Close"** (Закрыть)

### Шаг 3: Проверка установки

1. Откройте командную строку:
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter
2. В командной строке введите:
   ```bash
   python --version
   ```
3. Должна отобразиться версия Python (например, `Python 3.11.5`)
4. Также проверьте pip (менеджер пакетов):
   ```bash
   pip --version
   ```

Если команды не работают, возможно Python не добавлен в PATH. Переустановите Python с галочкой "Add Python to PATH".

---

## Установка PyCharm (рекомендуется)

PyCharm - это профессиональная среда разработки для Python, которая значительно упрощает работу с кодом.

### Почему стоит использовать PyCharm:

✅ **Удобный редактор кода** с подсветкой синтаксиса  
✅ **Встроенный терминал** для запуска команд  
✅ **Автодополнение кода** - помогает писать код быстрее  
✅ **Отладчик** - для поиска и исправления ошибок  
✅ **Встроенный Git** - для управления версиями кода  
✅ **Управление виртуальными окружениями** - изоляция проектов  
✅ **Интеграция с pip** - установка пакетов прямо из IDE  

### Шаг 1: Скачивание PyCharm

1. Перейдите на сайт JetBrains: **https://www.jetbrains.com/pycharm/download/**
2. Выберите версию:
   - **Community Edition** (бесплатная) - достаточно для наших целей
   - **Professional Edition** (платная, есть бесплатный пробный период)
3. Нажмите **"Download"** рядом с Community Edition

### Шаг 2: Установка PyCharm

1. Запустите установщик
2. Нажмите **"Next"** (Далее)
3. Выберите папку установки (можно оставить по умолчанию)
4. На странице "Installation Options" (Параметры установки):
   - ✅ Отметьте **"Create associations"** (Создать ассоциации) - для открытия .py файлов в PyCharm
   - ✅ Отметьте **"Add launchers dir to the PATH"** (Добавить в PATH)
5. Нажмите **"Install"** (Установить)
6. Дождитесь завершения и нажмите **"Finish"** (Готово)

### Шаг 3: Первый запуск PyCharm

1. Запустите PyCharm
2. При первом запуске выберите тему (Light/Dark) и нажмите **"Next: Featured plugins"**
3. Можно пропустить установку плагинов и нажать **"Start using PyCharm"**
4. Создайте новый проект:
   - Нажмите **"New Project"**
   - Выберите папку для проекта (например, `C:\Users\ВашеИмя\Desktop\Валидатор`)
   - Убедитесь, что выбран интерпретатор Python (должен определиться автоматически)
   - Нажмите **"Create"**

### Шаг 4: Открытие проекта в PyCharm

1. Если проект уже существует:
   - В PyCharm нажмите **"File" → "Open"**
   - Выберите папку с программой валидатора
   - Нажмите **"OK"**

---

## Установка необходимых модулей

Программа требует установки нескольких библиотек Python. Есть два способа установки:

### Способ 1: Через командную строку (быстрый способ)

1. Откройте командную строку (Win + R → `cmd`)
2. Перейдите в папку с программой:
   ```bash
   cd C:\Users\ВашеИмя\Desktop\Валидатор
   ```
   (Замените путь на ваш)
3. Установите модули:
   ```bash
   pip install -r requirements.txt
   ```
4. Дождитесь завершения установки

### Способ 2: Через PyCharm (удобный способ)

1. Откройте проект в PyCharm
2. Внизу окна найдите вкладку **"Terminal"** (Терминал)
3. В терминале введите:
   ```bash
   pip install -r requirements.txt
   ```
4. Нажмите Enter и дождитесь установки

### Способ 3: Установка каждого модуля отдельно

Если файл `requirements.txt` не работает, установите модули по отдельности:

```bash
pip install pandas
pip install openpyxl
pip install dnspython
```

### Проверка установки

В командной строке или терминале PyCharm выполните:

```bash
pip list
```

Должны быть видны установленные пакеты: `pandas`, `openpyxl`, `dnspython`.

---

## Подготовка файла с email адресами

### Формат файла

Программа работает с файлами Excel (`.xlsx` или `.xls`).

### Создание файла в Excel:

1. Откройте Microsoft Excel (или LibreOffice Calc, Google Sheets)
2. В первой строке создайте заголовок столбца:
   - Название может быть: **Email**, **E-mail**, **Почта**, **Mail**, **Адрес**
   - Или просто используйте первый столбец
3. В следующих строках введите email адреса (по одному в строке):
   ```
   Email
   example1@mail.ru
   example2@gmail.com
   test@yandex.ru
   ```
4. Сохраните файл:
   - **Файл → Сохранить как**
   - Выберите формат **Excel Workbook (*.xlsx)**
   - Назовите файл (например, `emails.xlsx`)
   - Сохраните в папку с программой валидатора

### Важные замечания:

- ✅ Один email адрес на строку
- ✅ Заголовок столбца не обязателен, но желателен
- ✅ Можно использовать несколько столбцов, программа найдет столбец с email
- ✅ Пустые строки будут пропущены

---

## Запуск программы

### Способ 1: GUI приложение (рекомендуется для начинающих)

1. Откройте командную строку (Win + R → `cmd`)
2. Перейдите в папку с программой:
   ```bash
   cd C:\Users\ВашеИмя\Desktop\Валидатор
   ```
3. Запустите GUI приложение:
   ```bash
   python email_validator_gui.py
   ```
4. В открывшемся окне:
   - Нажмите **"Выбрать файл"** и выберите Excel файл с email адресами
   - Выберите режим валидации (Строгий или Лояльный)
   - Настройте параметры проверки
   - Нажмите **"Начать проверку"**
   - После завершения нажмите **"Скачать результат"**

### Способ 2: Через командную строку (терминал)

#### Базовый запуск:

1. Откройте командную строку (Win + R → `cmd`)
2. Перейдите в папку с программой:
   ```bash
   cd C:\Users\ВашеИмя\Desktop\Валидатор
   ```
3. Запустите программу:
   ```bash
   python email_validator.py
   ```

#### Запуск с параметрами:

```bash
# Указать входной файл
python email_validator.py

# Указать входной и выходной файлы
python email_validator.py --input emails.xlsx --output results.xlsx

# Отключить SMTP проверку (быстрее)
python email_validator.py --input emails.xlsx --no-smtp

# Установить таймаут
python email_validator.py --input emails.xlsx --timeout 15
```

#### Интерактивный режим:

При запуске без параметров программа задаст вопросы:
1. Название файла (по умолчанию: `emails`)
2. Режим валидации (1 - строгий, 2 - лояльный)
3. Выполнять SMTP проверку? (да/нет)
4. Считать валидными catch-all почты? (да/нет)
5. Сколько почт проверить? (оставьте пустым для всех)

### Способ 3: Через PyCharm

1. Откройте файл `email_validator.py` в PyCharm
2. Нажмите правой кнопкой мыши на файл
3. Выберите **"Run 'email_validator'"** (Запустить)
   - Или нажмите `Shift + F10`
   - Или нажмите зеленую кнопку ▶️ рядом с кодом

### Режимы валидации

Программа поддерживает два режима валидации:

**Строгий режим (по умолчанию):**
- Только адреса с "Высокой" надежностью
- Обязательная активность email
- Обязательная доставляемость
- Проверка на подозрительные домены (typo-squatting)
- Проверка репутации домена отключена (может блокировать обычные домены)
- Максимальная точность (совпадение с Mailvalidator ~95-98%)

**Лояльный режим:**
- Адреса с "Высокой" и "Средней" надежностью
- Мягкие требования к активности и доставляемости
- Не проверяет репутацию домена
- Не проверяет подозрительные домены
- На 15-20% больше валидных адресов

### Интерактивный режим

После запуска программа задаст вам несколько вопросов:

#### 1. Введите название файла
```
Введите название файла (по умолчанию: emails): 
```
- Введите название вашего файла **без расширения** (например, `emails` или `my_emails`)
- Или просто нажмите Enter для использования файла `emails.xlsx`
- Программа автоматически найдет файл в папке с программой

#### 2. Выполнять SMTP проверку?
```
Выполнять SMTP проверку? (да/нет, по умолчанию: да): 
```
- **"да"** (или Enter) - полная проверка, более точная, но медленнее
- **"нет"** - быстрая проверка без SMTP, менее точная

#### 3. Считаем ли валидными catch-all почты?
```
Считаем ли валидными catch-all почты? (да/нет, по умолчанию: нет): 
```
- **"нет"** (или Enter) - catch-all адреса считаются невалидными (рекомендуется)
- **"да"** - catch-all адреса считаются валидными

**Что такое catch-all адрес?**  
Это почтовый ящик, который принимает письма на любые адреса домена, даже несуществующие. Например, если домен настроен как catch-all, то письма на `anything@domain.com` будут доставлены, даже если такого ящика нет.

#### 4. Сколько почт проверить?
```
Сколько почт из списка вы хотите проверить? (если нет ответа, то все): 
```
- Введите число (например, `100`) - проверит первые 100 адресов
- Или нажмите Enter - проверит все адреса из файла

### Процесс проверки

После ответов на вопросы программа начнет проверку:

```
[1/50] Проверка: example1@mail.ru
  Результат: ✅ ВАЛИДЕН - Надежность: Высокая
[2/50] Проверка: example2@gmail.com
  Результат: ❌ НЕВАЛИДЕН - Надежность: Нет
...
```

### Результат

После завершения проверки будет создан файл с результатами:
- Название: `название_входного_файла_YYYYMMDD_HHMMSS.xlsx`
- Например: `emails_20240120_143022.xlsx`

Файл будет сохранен в той же папке, где находится программа.

**Промежуточное сохранение:**
- Программа автоматически сохраняет промежуточные результаты каждые 1000 проверенных адресов
- Файлы имеют формат: `название_файла_checkpoint_1000.xlsx`
- Это позволяет не потерять результаты при сбое и отслеживать прогресс

---

## Понимание результатов

### Столбцы в результирующем файле:

1. **Email** - проверяемый адрес электронной почты
2. **Пользователь** - локальная часть email (до символа @)
3. **Домен** - доменная часть email (после символа @)
4. **Валидность** - можно ли использовать для рассылок
   - 🟢 **Да** (зеленый) - адрес валиден
   - 🟡 **Нет** (желтый) - адрес невалиден
5. **Надежность** - насколько уверены в доставке
   - 🟢 **Высокая** - все проверки пройдены
   - 🟢 **Средняя** - основные проверки пройдены
   - 🟡 **Нет** - проблемы с доставкой
6. **МХ-записи** - список почтовых серверов домена
7. **Корректность** - правильность синтаксиса email
   - 🟢 **Да** - синтаксис корректен
   - 🟡 **Нет** - синтаксис неверен
8. **Одноразовый (DEA)** - является ли одноразовым адресом
   - 🟢 **Нет** - обычный адрес
   - 🟡 **Да** - одноразовый адрес (не рекомендуется)
9. **Получение DNS, MX** - найдены ли DNS и MX записи
   - 🟢 **Да** - записи найдены
   - 🟡 **Нет** - записи не найдены
10. **Связь с SMTP-сервером** - удалось ли подключиться к почтовому серверу
    - 🟢 **Да** - подключение успешно
    - 🟡 **Нет** - не удалось подключиться
11. **Email активен** - существует ли почтовый ящик
    - 🟢 **Да** - ящик существует
    - 🟡 **Нет** - ящик не существует
    - ⚪ **–** - не удалось определить
12. **Доставляемость** - можно ли доставить письмо
    - 🟢 **Да** - доставка возможна
    - 🟡 **Нет** - доставка невозможна
    - ⚪ **–** - не удалось определить
13. **Catch-all адрес** - является ли catch-all адресом
    - 🟢 **Нет** - обычный адрес
    - 🟡 **Да** - catch-all адрес (может быть нежелателен)
    
    **Примечание:** Прочерк (–) в этом столбце означает, что не удалось определить catch-all адрес из-за технических ограничений (SMTP отключен, нет MX записей, ошибка подключения и т.д.). В таких случаях мы считаем адрес как "Нет" (не catch-all) для консервативной оценки, и ячейка помечается зеленым цветом. В результирующем файле прочерки заменяются на "Нет" для единообразия.
14. **Email переполнен** - переполнен ли почтовый ящик
    - 🟢 **Нет** - ящик не переполнен
    - 🟡 **Да** - ящик переполнен
15. **Ролевой аккаунт** - является ли служебным адресом (info@, support@ и т.д.)
    - 🟢 **Нет** - обычный адрес
    - 🟡 **Да** - служебный адрес
16. **Время проверки, сек** - сколько секунд заняла проверка
    - Обычно 1-3 секунды
    - 🟡 Желтым выделяются значения > 7 секунд (медленная проверка)
17. **Попыток проверки** - сколько раз проверялся адрес

### Цветовое кодирование:

- 🟢 **Зеленый** - положительный результат, все хорошо
- 🟡 **Желтый** - отрицательный результат или предупреждение
- ⚪ **Белый** - нейтральное значение или недостаточно данных

---

## Часто задаваемые вопросы

### Q: Программа не запускается, выдает ошибку "python не является внутренней или внешней командой"

**A:** Python не добавлен в PATH. Решение:
1. Переустановите Python
2. При установке обязательно отметьте галочку **"Add Python to PATH"**

### Q: Ошибка "ModuleNotFoundError: No module named 'pandas'"

**A:** Не установлены необходимые модули. Решение:
```bash
pip install pandas openpyxl dnspython
```

### Q: Ошибка "FileNotFoundError: Файл не найден"

**A:** Файл с email адресами не найден. Решение:
1. Убедитесь, что файл находится в той же папке, что и программа
2. Проверьте правильность названия файла
3. Убедитесь, что файл имеет расширение `.xlsx` или `.xls`

### Q: Проверка идет очень медленно

**A:** Это нормально. SMTP проверка занимает время. Решения:
1. Отключите SMTP проверку (ответьте "нет" на соответствующий вопрос)
2. Ограничьте количество проверяемых адресов
3. Проверяйте адреса порциями (например, по 100 штук)

### Q: Некоторые адреса показывают "Нет" для SMTP проверки, хотя они существуют

**A:** Некоторые почтовые серверы блокируют SMTP проверки для защиты от спама. Это нормально. Программа все равно проверит синтаксис и DNS записи.

### Q: Что делать, если программа зависла?

**A:** 
1. Подождите несколько минут (SMTP проверка может занимать время)
2. Если не помогло, закройте программу (Ctrl+C в командной строке)
3. Запустите снова с отключенной SMTP проверкой

### Q: Можно ли проверить адреса из CSV файла?

**A:** Да, но сначала нужно конвертировать CSV в Excel:
1. Откройте CSV файл в Excel
2. Сохраните как `.xlsx` формат

### Q: Как проверить только определенные адреса из большого списка?

**A:** При запуске программы на вопрос "Сколько почт проверить?" укажите нужное число.

### Q: Что означает "Catch-all адрес"?

**A:** Catch-all адрес - это почтовый ящик, который принимает письма на любые адреса домена. Например, если домен `example.com` настроен как catch-all, то письма на `anything@example.com`, `test123@example.com` и т.д. будут доставлены, даже если таких ящиков не существует. Обычно такие адреса нежелательны для рассылок, так как могут указывать на несуществующие ящики.

**Примечание:** Если SMTP проверка отключена или не удалось подключиться к серверу, программа не может определить catch-all адрес. В таких случаях адрес считается как "Нет" (не catch-all) для консервативной оценки.

### Q: Что такое промежуточное сохранение (checkpoint)?

**A:** Программа автоматически сохраняет промежуточные результаты каждые 1000 проверенных адресов. Это позволяет:
- Не потерять результаты при сбое программы
- Проверять прогресс при больших объемах данных
- Продолжить работу с последнего сохраненного места

Промежуточные файлы имеют формат: `название_файла_checkpoint_1000.xlsx`, где 1000 - количество проверенных адресов.

---

## Дополнительные советы

### Оптимизация работы:

1. **Используйте PyCharm** - это значительно упростит работу
2. **Проверяйте порциями** - для больших списков проверяйте по 100-200 адресов за раз
3. **Делайте резервные копии** - сохраняйте оригинальные файлы с email адресами
4. **Проверяйте логи** - файл `email_validator.log` содержит подробную информацию о проверках

### Безопасность:

- Программа не отправляет реальные письма
- Все проверки выполняются локально на вашем компьютере
- Email адреса не передаются третьим лицам

---

## Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Часто задаваемые вопросы"
2. Посмотрите файл `email_validator.log` для деталей ошибок
3. Убедитесь, что все модули установлены правильно
4. Проверьте, что Python версии 3.7 или новее

---

**Удачной работы с Email Валидатором!** 🚀
